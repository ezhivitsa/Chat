define(['elements/header', 'elements/userName'], 
	function (Header, UserName) {
		function UserPage() {
			var header = new Header(),
				userName = new UserName();

			//userName.
		}

		return UserPage;
	}
);